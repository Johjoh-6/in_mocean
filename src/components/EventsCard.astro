---
import { Image } from "astro:assets";
import { t, type Locale } from "../translations";
import { getIcon } from "../utils/icons";
import { parseDate } from "../utils/date";
import type { Event } from "../content.config";

interface Props {
	locale: Locale;
	event: Event;
	index: number;
}

const { event, locale, index } = Astro.props;

const isLocalImage = typeof event.image !== "string";

const getMonth = (dateString: string): string => {
	const date = parseDate(dateString);
	const month = date.getMonth();
	return t(locale, "date.months." + month);
};

const getDateNumber = (dateString: string): number => {
	const date = parseDate(dateString);
	return date.getDate();
};

const getTime = (dateString: string): string => {
	const date = parseDate(dateString);
	const hours = date.getHours();
	const minutes = date.getMinutes();
	return `${hours}:${minutes.toString().padStart(2, "0")}`;
};

const getTwoLastNumberOfYear = (dateString: string): number => {
	const date = parseDate(dateString);
	return date.getFullYear() % 100;
};

const isSameDay = (dateStringA: string, dateStringB: string): boolean => {
	const a = parseDate(dateStringA);
	const b = parseDate(dateStringB);
	return (
		a.getFullYear() === b.getFullYear() &&
		a.getMonth() === b.getMonth() &&
		a.getDate() === b.getDate()
	);
};

const getShortDate = (dateString: string): string => {
	return `${getDateNumber(dateString)} ${getMonth(dateString)}`;
};

const MapPin = getIcon("mapPin");
const Clock = getIcon("clock");
const ArrowRight = getIcon("arrow-right");

const url = event.url;
const hasUrl = !!url;
const Tag = hasUrl ? "a" : "div";
const sameDay = isSameDay(event.dateStart, event.dateEnd);

const accentColors = [
	{
		gradient: "from-brand-pink-500 to-brand-purple-600",
		border: "hover:border-brand-pink-500/50",
	},
	{
		gradient: "from-brand-cyan-400 to-brand-blue-600",
		border: "hover:border-brand-cyan-400/50",
	},
	{
		gradient: "from-brand-amber-400 to-brand-orange-600",
		border: "hover:border-brand-amber-400/50",
	},
	{
		gradient: "from-brand-emerald-400 to-brand-teal-600",
		border: "hover:border-brand-emerald-400/50",
	},
];
const accent = accentColors[index % accentColors.length];
---

<Tag
	class:list={[
		"group relative bg-surface-card backdrop-blur-sm rounded-2xl overflow-hidden border border-border-default transition-all duration-300 block",
		hasUrl ? `${accent.border} cursor-pointer` : "border-border-default/60",
	]}
	{...hasUrl ? { href: url, target: "_blank" } : {}}
>
	<div class="flex flex-col md:flex-row items-stretch">
		<div
			class="text-center gap-2 md:gap-1 md:w-24 flex md:flex-col items-center justify-center p-4 bg-linear-to-br from-brand-pink-500 to-brand-purple-600 text-text-primary"
		>
			<div class="text-3xl md:text-4xl font-bold">
				{getDateNumber(event.dateStart)}
			</div>
			<div class="text-1xl md:text-base uppercase tracking-wider">
				{getMonth(event.dateStart)}
			</div>
			<div class="text-3xl md:text-4xl font-black">
				{getTwoLastNumberOfYear(event.dateStart)}
			</div>
		</div>

		<div class="flex-1 flex flex-col md:flex-row">
			<div class="flex-1 p-4 md:p-5">
				<h3
					class:list={[
						"text-xl md:text-2xl text-text-primary mb-3 transition-colors",
						hasUrl && "group-hover:text-brand-pink-400",
					]}
				>
					{event.title}
				</h3>

				<div class="space-y-2 mb-4">
					<div
						class="flex items-center gap-2 text-text-muted text-sm"
					>
						<MapPin class="w-4 h-4 text-brand-pink-400 shrink-0" />
						<span
							>{event.location}, {event.city}, {
								event.country
							}</span
						>
					</div>
					<div
						class="flex items-center gap-2 text-text-muted text-sm"
					>
						<Clock class="w-4 h-4 text-brand-pink-400 shrink-0" />
						{
							sameDay ? (
								<span>
									{getTime(event.dateStart)} –{" "}
									{getTime(event.dateEnd)}
								</span>
							) : (
								<span>
									{getTime(event.dateStart)}{" "}
									<span class="text-text-faint">
										({getShortDate(event.dateStart)})
									</span>{" "}
									– {getTime(event.dateEnd)}{" "}
									<span class="text-text-faint">
										({getShortDate(event.dateEnd)})
									</span>
								</span>
							)
						}
					</div>
				</div>

				{
					hasUrl && (
						<p class="flex items-center gap-2 text-brand-pink-400 hover:text-brand-pink-300 transition-colors group text-sm">
							<span>{t(locale, "events.details")}</span>
							<ArrowRight class="w-4 h-4 group-hover:translate-x-1 transition-transform" />
						</p>
					)
				}
			</div>

			<div class="md:w-48 h-32 md:h-auto relative overflow-hidden">
				{
					isLocalImage ? (
						<Image
							src={event.image}
							alt={event.title}
							widths={[192, 384]}
							sizes="(min-width: 768px) 192px, 100vw"
							class="w-full h-full object-cover"
							loading="lazy"
							decoding="async"
						/>
					) : (
						<img
							src={event.image}
							alt={event.title}
							class="w-full h-full object-cover"
							loading="lazy"
							decoding="async"
						/>
					)
				}
				<div
					class="absolute inset-0 bg-linear-to-br from-surface-card-solid/80 to-transparent md:from-transparent md:to-surface-card-solid/0"
				>
				</div>
			</div>
		</div>
	</div>

	<div
		class:list={[
			`absolute bottom-0 left-0 right-0 h-1 bg-linear-to-br ${accent.gradient} origin-left`,
			!hasUrl && "opacity-40",
		]}
	>
	</div>
</Tag>
